import {
  grilleLoop
} from 'echecs/helpers/grille-loop';

module('GrilleLoopHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = grilleLoop(42);
  ok(result);
});
